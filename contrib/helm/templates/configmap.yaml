apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taguette.fullname" . }}
  labels:
    {{- include "taguette.labels" . | nindent 4 }}
data:
  # TODO: Move settings from configFile blob to distinct values
  config.py: |
    {{- .Values.configFile | nindent 4 }}
